# 需求

- Claude Code 用户通过 `/plugin` 统一管理 MCP 与 Skill，不再分别追踪 npm/Skill 版本。
- Claude MCP `cua_status` 默认离线报告当前版本；可显式请求检查最新已发布版本与升级方式。
- 更新检查不安装依赖、不改配置、授权或系统权限；失败保留本地状态。
- 保持现有手动安装和 pi 入口兼容；本次不增加自制更新器、不执行发布或迁移用户安装。

验收：Plugin 元数据版本与 MCP 固定版本一致，Skill 与旧入口一致；默认离线、更新可用/无更新/失败/取消可测；编译、测试和安装包检查通过。
